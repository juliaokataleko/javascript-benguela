{
    // 3 categories of operators,
    // unary operatores (!), 
    // binary operatores +, -, 
    // ternary operatores ()

    let name    = prompt("What is your name?");

    // let points  = name === "Kataleko" ? 10 : 0;

    // console.log(points);

    name === "Kataleko" ? console.log(10) : console.log(0);
    
}