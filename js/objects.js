let Person = {
    name: "Julião Kataleko",
    age: 25,
    favFood: "Arroz com feijão -_-",
    fun: function() {
        console.log("Yeahhhh");
    }
}

Person.fun();

let now = new Date();
let grades = [20, 10, 43, 5, 10];
grades.push(23);    // adiciona um elemento em um array
grades.shift()      // remove o primeiro elemento de um array
grades.shift()      // remove o primeiro elemento de um array
grades.shift()      // remove o primeiro elemento de um array
console.log(grades)

{
    
    let myName = "July";
    myName = myName.toUpperCase();
    console.log(myName);

    let yourName = new String("Isabel");
    console.log(typeof(yourName));

    let age = new Number();
    age.valueOf();
    
}